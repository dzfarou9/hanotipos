# Flutter/Firebase consumer rules are bundled with their dependencies.
# Add any project-specific keep rules below.

# Keep google_mlkit native bindings if needed
-keep class com.google.mlkit.** { *; }