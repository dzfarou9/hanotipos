// lib/config/qr_login_config.dart

/// إعدادات تسجيل الدخول عبر رمز QR.
///
/// ملاحظة أمان (C-1): `appEncryptionKey` مضمّن في التطبيق ومشارك بين كل
/// نسخه، فهو إخفاء (obfuscation) وليس حماية كاملة. ملح كل حساب يجعل مفتاح
/// التشفير مختلفاً لكل حساب (الإصدار 5 من الصيغة)، لكن من يفكّك APK يستطيع
/// استخراج السر الأساسي وقراءة الملح من الرمز نفسه — لذلك الرمز الدائم
/// يوسّع نافذة الضرر عند تسرّب صورته، وصمام الأمان هو إعادة توليد الملح
/// من شاشة الإعدادات (رمز جديد = كل النسخ القديمة تتوقف عن العمل).
///
// TODO(C-1): التصميم المستهدف (يحتاج Cloud Functions + باك-إند منشور):
//   1) جهاز العرض يطلب من الخادم رمز دخول مرتبطاً بالحساب، والخادم يخزّنه
//      بحالة "غير مستخدم" أو "نشط" مع إمكانية الإلغاء من الخادم.
//   2) الرمز الذي يُطبع في QR يحتوي التوكن فقط — لا كلمة المرور أبداً،
//      فلا يوجد سرّ قابل للسرقة داخل APK ولا داخل الصورة.
//   3) جهاز المسح يرسل التوكن إلى الخادم الذي يتحقق من حالته (نشط وغير
//      ملغى) ثم يُنشئ جلسة (Firebase custom token) للجهاز.
//   4) الإلغاء والتدوير يتم في الخادم، فلا اعتماد على ساعة الجهاز.
//   حتى ذلك الحين تبقى الإجراءات الحالية (ملح لكل حساب + رمز دائم
//   قابل للإلغاء بإعادة التوليد + رفض الإصدارات القديمة) حلاً مؤقتاً.
class QrLoginConfig {
  QrLoginConfig._();

  // ⭐ السر الأساسي (32 بايت UTF-8) — يُخلط مع ملح الحساب عبر SHA-256
  // لإنتاج مفتاح AES مختلف لكل حساب/تثبيت.
  static const String appEncryptionKey = 'HanotiPOS-QR-Login-Key-2026-ABCD';
}
